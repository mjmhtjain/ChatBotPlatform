{
  "name": "integration",
  "private": true,
  "scripts": {
    "test:pact:consumer": "vitest run --config vitest.config.ts",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@pact-foundation/pact": "^13.1.4",
    "@playwright/test": "^1.44.0",
    "axios": "^1.7.2",
    "vitest": "^2.0.5"
  }
}
